import { Container } from '@components';
import { Fragment } from 'react';

const orders = () => {
  return (
    <Fragment>
      <Container main>Orders</Container>
    </Fragment>
  );
};

export default orders;
