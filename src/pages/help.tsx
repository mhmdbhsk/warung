import { Container } from '@components';
import { Fragment } from 'react';

const orders = () => {
  return (
    <Fragment>
      <Container main>Help</Container>
    </Fragment>
  );
};

export default orders;
